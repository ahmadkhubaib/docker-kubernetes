version: "3"
services:
  my-react:
    build: .
    ports:
      - "9876:3000"
    volumes:
      - /app/node_modules
      - ./:/app
  tests:
    build: .
    volumes:
      - /app/node_modules
      - ./:/app
    command: ["npm", "run", "test"]
